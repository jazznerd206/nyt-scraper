<nav class="top-bar topbar-responsive">
  <div class="top-bar-title">
    <span data-responsive-toggle="topbar-responsive" data-hide-for="medium">
      <button class="menu-icon" type="button" data-toggle></button>
    </span>
    <a class="topbar-responsive-logo" href="#"><strong>Mongo Scraper</strong></a>
  </div>
  <div id="topbar-responsive" class="topbar-responsive-links">
    <div class="top-bar-left">
      <ul class="menu simple vertical medium-horizontal">
        <li><a href="/">Home</a></li>
        <li><a href="/saved" id='saved-articles'>Saved Articles</a></li>
        <div class="button">
            <li><a href="/scrape" id="scrape">World News</a></li>
        </div>
        <div class="button" id="delete">
            <li><a href="/delete-articles">Clear Articles</a></li>
        </div>
      </ul>
    </div>
  </div>
</nav>

<div class="title-container animated fadeIn">
  <h1>NEW ARTICLES</h1>
</div>

<div class="grid" id="article-container">
    <div class="grid-x grid-margin-x grid-margin-y">
    {{#if articles}}
      {{#each articles}}
        {{#unless saved}}
          <div class="cell medium-3  small-6 x-small-12">
            <div class="card animated tada" data-id={{_id}} style="width: 300px;">
              <p><button class="button expanded" data-open="exampleModal1{{_id}}">Add Note</button></p>
                <div class="reveal" id="exampleModal1{{_id}}" data-reveal>
                  <input class="titleinput" type="text" value="" placeholder="Enter Name">
                  <input class="bodyinput" type="text" value="" placeholder="Enter Note">
                  <div class="button" value="submit" id="savenote" data-id="{{_id}}" data-close>Submit</div>
                  <div class="button float-right" value="delete" id="deletenote" data-id="{{_id}}" data-close>Delete</div>
                  <button class="close-button" data-close aria-label="Close modal" type="button">
                    <span aria-hidden="true">&times;</span>
                  </button>
                </div>
                <div class="card-divider">
                    <h4><a href="{{link}}">{{title}}</a></h4>
                </div>
                {{#if articles.note}}
                  <p>{{articles.note.body}}</p>
                {{/if}}
                <div class="button save-article" data-id="{{_id}}">
                  <span>Save</span>
                </div>
            </div>
        </div>
        {{/unless}}
      {{/each}}
      {{else}}
        <div class="nothing-here-container title-container animated fadeIn">
          <h1>Oops, no articles here. Try scraping new ones.</h1>
        </div>
    {{/if}}
    </div>
</div>



<div class="footer">
    <p>NYT Mongo Scraper</p>    
</div>




